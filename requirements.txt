# Core runtime dependencies for this app (Python 3.8 on Windows).
numpy==1.24.4
opencv-python==4.10.0.84
paramiko==3.5.1
psycopg2-binary==2.9.9

# Optional utility stack (`utilities/results.py`) is intentionally excluded here
# because current workstation Python is 32-bit and recent torch/ultralytics
# wheels are not available for this platform.
